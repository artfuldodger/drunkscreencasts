<h1>Sign in!</h1>

<%= simple_form_for @user, url: sessions_path, method: :post,
      html: { class: 'form' } do |f| %>
  <%= f.input :login %>
  <%= f.input :password %>

  <%= f.submit 'Sign in', class: 'btn btn-default' %>
<% end %>
